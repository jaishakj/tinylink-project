# Example environment variables
# For SQLite (default, included):
DATABASE_URL=file:./data/tinylink.db

# For Postgres (if deploying to Neon or others), set:
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# Base public URL (used for generating short links)
BASE_URL=http://localhost:3000

# Port
PORT=3000
